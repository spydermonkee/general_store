<h2><%= @product.name %></h2>
<h4><%= @product.price %></h4>
<p><%= @product.description %></p>

<br>
<p><%= link_to 'Back to Products', products_path %> | <%= link_to 'Edit', edit_product_path(@product.id) %> | <%= link_to 'Delete', product_path(@product.id), method: :delete, confirm: 'Are you sure?' %></p>
